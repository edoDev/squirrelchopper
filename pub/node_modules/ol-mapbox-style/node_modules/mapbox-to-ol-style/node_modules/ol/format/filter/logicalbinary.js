import _ol_ from '../../index';
import _ol_format_filter_Logical_ from '../filter/logical';

/**
 * @classdesc
 * Abstract class; normally only used for creating subclasses and not instantiated in apps.
 * Base class for WFS GetFeature binary logical filters.
 *
 * @constructor
 * @param {!string} tagName The XML tag name for this filter.
 * @param {!ol.format.filter.Filter} conditionA First filter condition.
 * @param {!ol.format.filter.Filter} conditionB Second filter condition.
 * @extends {ol.format.filter.Logical}
 */
var _ol_format_filter_LogicalBinary_ = function(tagName, conditionA, conditionB) {

  _ol_format_filter_Logical_.call(this, tagName);

  /**
   * @public
   * @type {!ol.format.filter.Filter}
   */
  this.conditionA = conditionA;

  /**
   * @public
   * @type {!ol.format.filter.Filter}
   */
  this.conditionB = conditionB;

};

_ol_.inherits(_ol_format_filter_LogicalBinary_, _ol_format_filter_Logical_);
export default _ol_format_filter_LogicalBinary_;
